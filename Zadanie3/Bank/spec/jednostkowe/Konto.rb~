require_relative "../../lib/Czlowiek"
require "../spec_helper"

RSpec.describe Bank::Konto do
  describe '#inicjowanie' do
    it ' ' do
      konto = Bank::Konto.new
      expect(0).to eq(konto.kwota)
      expect(konto.id).to be > 1
      expect(konto.id).to be <= 1000
    end
  end
end
